/**
 * 클래스 기본 문법
 *
 * ex)
 *      class Person {
 *          constructor(name, skill) {
 *              this.name = name;
 *              this.skill = skill;
 *          }
 *
 *          sayHi() {
 *             console.log('hi');
 *          }
 *      }
 *
 * => 이 Person 클래스 코드는 name과 skill 값을 받아 객체를 생성할 수 있게 생성자 메소드(constructor)를 선언하고
 *    sayHi()라는 클래스 메소드(class method)를 선언한 코드이다. 여기에서 name과 skill 속성을 클래스 필드(class field)또는
 *    클래스 속성(class property)라고 한다.
 * => 클래스도 앞의 생성자 함수와 동일하게 new 키워드를 붙여 객체를 생성한다.
 * => 이렇게 클래스로 생성된 객체를 클래스 인스턴스(class instance) 라고 한다.
 *
 */
