/**
 * 타입스크립트의 클래스
 *
 * - 타입스크립트로 클래스를 작성할 때는 생성자 메소드에 사용될 클래스 속성들을 미리 정의해 주어야한다.
 *
 *      ex)
 *          class ChatGpt {
 *              name: string;
 *              constructor(name: string) {
 *                  this.name = name;
 *              }
 *
 *              sum(a: number, b: number):number {
 *                  return a + b;
 *              }
 *          }
 *
 *
 */
